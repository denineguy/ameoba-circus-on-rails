<%= form_for(@ameoba) do |f| %>
  <% if @ameoba.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ameoba.errors.count, "error") %> prohibited this ameoba from being saved:</h2>

      <ul>
      <% @ameoba.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <p>
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </p>
    <p>
      <%= f.label :talent %><br>
      <%= f.select :talent_id, options_for_select(@talents.map{ |s| [s.name, s.id] }) %>
    </p>
    <p>
      <%= f.label :act %><br>
      <%= f.select :act_id, options_for_select(@acts.map{ |s| [s.name, s.id] }) %>
    </p>
    <%= f.submit %>
  </div>
<% end %>